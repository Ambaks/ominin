# URL publique du backend (service Render).
BACKEND_URL=https://<service>.onrender.com

# Jeton de l'appareil, affiché une seule fois par l'éditeur SQL Supabase :
#   select omilink_provision_device('<uuid établissement>', 'Cuisine');
DEVICE_TOKEN=
